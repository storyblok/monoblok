export function PreviewBanner() {
  return (
    <div
      style={{
        background: "#7C3AED",
        color: "white",
        padding: "8px 16px",
        textAlign: "center",
        fontSize: "14px",
      }}
    >
      Preview Mode — live updates from the Visual Editor are active.
    </div>
  );
}

export default PreviewBanner;
