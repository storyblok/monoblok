---
import { type SbRichTextNode, type SbAstroRendererOptions, normalizeNodes } from '../';
import RenderChildren from './richtext/RenderChildren.astro';

interface Props extends SbAstroRendererOptions {
  document: SbRichTextNode | null | undefined | SbRichTextNode[];
}

const { document, components } = Astro.props;

const nodes = normalizeNodes(document);
---

<RenderChildren nodes={nodes} options={{ components }} />
