---
import type { SbRichTextProps } from '../../src/index';

type Props = SbRichTextProps<'heading'>;

const { attrs } = Astro.props;
---

<p data-type="custom-heading" data-level={attrs?.level}><slot /></p>
