---
import type { SbRichTextProps } from '../../src/index';

type Props = SbRichTextProps<'code_block'>;

const { attrs } = Astro.props;
---

<pre class={`language-${attrs?.class}`}><code data-lang={attrs?.class}><slot /></code></pre>
