{
  "name": "@storyblok/playground-schema-astro",
  "projectType": "application",
  "targets": {
    "build": {
      "dependsOn": ["^build"],
      "command": "pnpm build",
      "options": {
        "cwd": "{projectRoot}"
      },
      "outputs": ["{projectRoot}/dist"]
    }
  }
}
