import { defineDatasource } from "@storyblok/schema";

export const sectionThemesDatasource = defineDatasource({
  name: "Section Themes",
  slug: "section_themes",
});

export const faqCategoriesDatasource = defineDatasource({
  name: "FAQ Categories",
  slug: "faq_categories",
});

export const currenciesDatasource = defineDatasource({
  name: "Currencies",
  slug: "currencies",
});
