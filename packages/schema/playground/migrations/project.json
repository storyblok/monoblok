{
  "name": "@storyblok/playground-schema-migrations",
  "projectType": "application",
  "targets": {
    "build": {
      "dependsOn": ["^build"],
      "command": "pnpm build",
      "options": {
        "cwd": "{projectRoot}"
      },
      "outputs": ["{projectRoot}/dist"]
    }
  }
}
