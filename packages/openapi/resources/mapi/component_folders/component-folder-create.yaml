type: object
description: Input for creating a component folder.
required:
  - name
properties:
  name:
    type: string
    description: The name of the component folder
  parent_id:
    type: integer
    description: ID of the parent folder
